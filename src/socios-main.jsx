import React from "react";
import ReactDOM from "react-dom/client";
import SociosApp from "./socios/SociosApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SociosApp />
  </React.StrictMode>
);
